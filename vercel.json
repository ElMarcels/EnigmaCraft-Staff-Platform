{
  "buildCommand": "prisma generate && prisma migrate deploy && node --experimental-strip-types prisma/seed.ts && next build",
  "crons": [
    {
      "path": "/api/cron/backup",
      "schedule": "0 6 * * *"
    },
    {
      "path": "/api/cron/contact-reminder",
      "schedule": "0 8 1 * *"
    }
  ]
}